services:
  - type: web
    name: backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node dist/server.js
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PORT
        value: 5000